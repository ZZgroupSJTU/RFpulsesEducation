function cmap = interpColorMap(cPos, cZero, cNeg, numSteps, epsilon)
% Creates a color map by interpolating between the colors cPos, cZero,
% cNeg, where epsilon is a parameter which determines the "strength" of the
% interpolation.
